site_name: ukp_template_2
nav:
  - Home: index.md

theme:
  name: material

plugins:
  - search
  - mkdocstrings:
      project_name: ukp_template_2
      handlers:
        python:
          options:
            docstring_style: numpy